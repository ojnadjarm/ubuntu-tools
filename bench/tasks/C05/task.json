{
  "id": "C05",
  "tier": "change",
  "set": "night",
  "title": "mute the test output",
  "prompt": "mute that PCBENCH test output sink, I don't want to hear it",
  "answer_schema": {
    "type": "object",
    "required": ["sink", "muted"],
    "properties": {
      "sink": {"type": "string", "description": "the sink you muted"},
      "muted": {"type": "boolean", "description": "is it muted now"},
      "rollback": {"type": "string", "description": "the exact command that unmutes it"},
      "did_it": {"type": "boolean"},
      "unknown": {"type": "boolean"},
      "reason": {"type": "string"}
    }
  },
  "allowed_side_effects": ["null_modules", "ledger_lines"],
  "needs": [],
  "budget": {"seconds": 480, "usd": 1.5, "screenshots": 0},
  "oracle_s": 1,
  "tags": ["audio", "pc audio", "pc undo", "ledger"]
}
