Copy this directory to `tasks/<ID>/`, set `id` to the directory name, and fill the five
scripts. Rules and field meanings: `~/agents/bench/SCHEMA.md`. Verify with
`pcbench validate --task <ID> --oracle` and `pcbench run --task <ID> --dry-run`.
