{
  "id": "O02",
  "tier": "observe",
  "set": "night",
  "title": "Where is the sound going?",
  "prompt": "which output is the sound going to right now?",
  "answer_schema": {
    "type": "object",
    "required": ["sink", "is_buds"],
    "properties": {
      "sink": {"type": "string", "description": "the raw PipeWire/PulseAudio sink name exactly as `pactl get-default-sink` or `pc audio --json .default_sink` prints it, e.g. alsa_output.pci-0000_00_1f.3.analog-stereo", "pattern": "^[a-z0-9_.:-]+$"},
      "is_buds": {"type": "boolean"},
      "unknown": {"type": "boolean"},
      "reason": {"type": "string"}
    }
  },
  "allowed_side_effects": [],
  "needs": [],
  "budget": {"seconds": 240, "usd": 1.0, "screenshots": 0},
  "oracle_s": 0,
  "tags": ["audio", "pc audio", "night-safe"]
}
