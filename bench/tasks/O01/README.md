# O01 — is the TV on?

pc path: `eye tv` (mode/override, no window). Raw path: none exists that is honest — a raw
`pc shot`/`wpctl`/`ddcutil` guess is exactly the disturbance this task forbids at night, so
there is no legitimate raw alternative; that asymmetry is the point of the task.
