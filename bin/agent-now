#!/usr/bin/env bash
# Run one agent immediately through systemd.
set -euo pipefail
name="${1:-}"
[ -n "$name" ] || { echo "usage: agent-now <name>" >&2; exit 2; }
systemctl --user start --no-block "agent@$name.service"
echo "agent $name started"
