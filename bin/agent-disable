#!/usr/bin/env bash
# Disable and stop one agent's timer.
set -euo pipefail
name="${1:-}"
[ -n "$name" ] || { echo "usage: agent-disable <name>" >&2; exit 2; }
systemctl --user disable --now "agent@$name.timer" 2>/dev/null || true
echo "agent $name disabled"
