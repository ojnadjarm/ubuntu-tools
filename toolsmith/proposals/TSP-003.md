# TSP-003 — O02 `sink` needs a description (raw sink name), 0/6 both arms

## Problem
O02 fails every trial in both arms because the schema's `sink` is an undescribed string; agents answer a human label, `check.sh` string-matches `pactl get-default-sink`.

## Evidence
- `bench/tasks/O02/task.json:9` — `"sink": {"type": "string"}`, no description.
- BASELINE-PCBENCH.md:257-266 — 0/6 both arms, answers like "Built-in Audio Digital Stereo (HDMI)"; `pcbench validate --oracle O02` passes.
- Same shape as C04's fixed schema (`bench/tasks/C04/task.json` `state` enum + description took C04 0/3 → 3/3).

## Survey
- exists: structured-output work shows JSON pass rate ≫ value accuracy and that per-field natural-language descriptions are the lever, https://arxiv.org/html/2604.25359v1 and https://github.com/guidance-ai/jsonschemabench (read 2026-09-07). Rejected: a normalized comparison in `check.sh` (weakens the oracle; SCHEMA.md rule 4).
- chosen: description on `sink`: "the raw PipeWire/PulseAudio sink name exactly as `pactl get-default-sink` or `pc audio --json .default_sink` prints it, e.g. alsa_output.pci-…", plus `pattern: "^[a-z0-9_.:-]+$"`.
- why: task fix, checker untouched, mirrors the proven C04 fix.

## Proposed change
Edit `bench/tasks/O02/task.json` only; run `pcbench validate --oracle O02` (fixture, no arm).

## Success check
- metric: O02 pass, both arms, n=3
- before: 0/3 new, 0/3 old
- target: ≥2/3 new
- measured by: `pcbench report bench/runs/<sun> --task O02`

## Effort
- model: sonnet
- sessions: 1
- resident: none

## Class
auto-ok

## Result
Files touched: `bench/tasks/O02/task.json` — added `description` and `pattern` to `sink`.

Validation: `pcbench validate --task O02` → `O02  OK` / `1/1 tasks valid`;
`pcbench validate --task O02 --oracle` → same, `solve.sh` still passes `check.sh` and a wrong
canned answer still fails it.

Before: O02 pass 0/3 new, 0/3 old (BASELINE-PCBENCH.md).
After: measurement pending on `pcbench-weekly` (Sun 2026-09-13 02:00) — `pcbench away` reports
present now, so no trial was run this session (owner rule, footer item 6).

`pc doctor --quick`: PASS 13/13 before and after.
