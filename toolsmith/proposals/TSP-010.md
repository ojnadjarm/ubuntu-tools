# TSP-010 — `moodle-keep` honours `MOODLE_ENVS_DIR`

## Problem
`moodle-keep` hardcodes the envs root while `config.example.env` documents `MOODLE_ENVS_DIR`; a relocated envs dir silently backs up nothing.

## Evidence
- `bin/moodle-keep:7` — `ROOT="$HOME/moodle-envs"`.
- `config.example.env:20` — `# MOODLE_ENVS_DIR=$HOME/moodle-envs`.

## Survey
- exists: nothing external; other fleet scripts read config through `fleetlib.sh`.
- chosen: `ROOT="${MOODLE_ENVS_DIR:-$HOME/moodle-envs}"`.
- why: one line.

## Proposed change
`bin/moodle-keep:7`; a case in `bin/tests/moodle-keep.test.sh` with a temp `MOODLE_ENVS_DIR` holding a fake env dir (no docker call).

## Success check
- metric: envs found under a temp `MOODLE_ENVS_DIR`
- before: 0
- target: 1
- measured by: `bash bin/tests/moodle-keep.test.sh`

## Effort
- model: sonnet
- sessions: 1
- resident: none

## Class
auto-ok
